// @ts-check
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you were using any experimental flags in TS, add them here under 'experimental'
  // experimental: {},
};

module.exports = nextConfig;
